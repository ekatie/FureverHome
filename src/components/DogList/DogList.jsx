import React from "react";

const DogsList = () => {
  return (
    <div>
      <h1>Dog List</h1>
      {/* {dogs.map((dog) => (
        <div key={dog.id}>
          <Link to={`/dogs/${dog.id}`}>
            <h3>{dog.name}</h3>
          </Link>
          <p>{dog.default_image}</p>
        </div>
      ))} */}
    </div>
  );
};

export default DogsList;
